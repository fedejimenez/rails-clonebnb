<div id="clearance" class="sign-up form-group">
    <h2><%= t(".title") %></h2>

  <%= form_for User.new do |form| %>
	<%= render partial: '/users/form', object: form %>

    <div class="submit-field">
      <%= form.submit %>
    </div>

    <div class="other-links">
      <%= link_to t(".sign_in"), sign_in_path %>
    </div>
  <% end %>

</div>


